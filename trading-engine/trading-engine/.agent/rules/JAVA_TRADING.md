---
trigger: always_on
---

# MISSION PARAMETERS
CRITICAL: This project is a Java-based IBKR TWS API trading application.
CRITICAL: The agent must ONLY use tools, skills, and libraries related to:
          - Java (JDK 21+)
          - Maven/Gradle build systems
          - Interactive Brokers (IBKR) TWS API (EClient, EWrapper)
          - Polygon.io REST/WebSocket APIs

# RESTRICTED BEHAVIOR
CRITICAL: Do NOT load, reference, or attempt to use any plugins or skills from 
          the "science" or "life-science" categories (e.g., AlphaFold, ChEMBL, 
          NCBI, UniProt, or literature-search plugins). 
CRITICAL: If the agent encounters terms like "protein", "genome", "clinical", 
          "ontology", or "bioactive", it must ignore them and prioritize the 
          trading/software development context.
CRITICAL: Do not perform any web searches for scientific literature.
